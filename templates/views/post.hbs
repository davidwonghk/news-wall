<div class="container">
	<div class="row">
		<div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">

			<article>
				<header>
					<h1>{{{data.post.Title}}}</h1>
					<h5>
						{{# each data.post.categories}}
						<a href="{{ categoryUrl key }}">#{{ name }} </a>
						{{/each}}
					</h5>
				</header>
				<div class="post">
					<!--
					{{#if data.post.image}}<img src="{{{imageUrl data.post.image}}}" />{{/if}}
				-->
					<div class="share">
					<div class="fb-like"
						data-href="{{data.post.url}}"
						data-layout="standard"
						data-action="like"
						data-size="large"
						{{!--data-share="true"--}}
						data-show-faces="true">
					</div>
					{{> share}}
					</div>
					<hr>
						{{{data.html}}}
					<hr>
					<div class="share">
						{{> share}}
					</div>

				</div>
			</article>
			<hr>

		</div>
	</div>
</div>

<div class="overlay">

</div>
